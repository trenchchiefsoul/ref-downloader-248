# Required runtime
playwright>=1.40

# Optional. Used for DOI extraction from PDF text when:
#   - Input is a PDF, AND
#   - [zotero].db_path is not configured / Zotero lookup returned empty
# If you only feed DOIs, or your PDFs are all in Zotero with DOI metadata,
# skip this. Comment out the next line in that case.
pymupdf>=1.23
